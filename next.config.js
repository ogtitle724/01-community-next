/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    logging: "verbose",
  },
  /* experimental: {
    logging: {
      level: 'verbose'
    }
  } */
};

module.exports = nextConfig;
